version: '3.3'
services:
    spa:
        network_mode: web
        container_name: spa
        image: spaapp:latest
        # env_file:
        #     - .env
        environment:
            - BASEURL=$BASEURL
            - HOSTNAME=$HOST
            - KEY=$KEY